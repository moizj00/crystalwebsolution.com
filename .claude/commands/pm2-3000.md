Start crystal-web-3000 (3000) and open logs.
```bash
cd "C:/Users/Sales/Crystal Web Solution" && pm2 start ecosystem.config.cjs --only crystal-web-3000 && start wt.exe -d "C:/Users/Sales/Crystal Web Solution" pwsh -NoExit -c "pm2 logs crystal-web-3000"
```
