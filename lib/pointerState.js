// Shared pointer coordinates for decorative Canvas objects. CameraRig owns
// the only window listener and writes here because the fixed Canvas has
// pointer-events disabled.
export const pointerState = { x: 0, y: 0 };
